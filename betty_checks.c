#include <stdio.h>
#include <main.h>

/**
 * Main - Simple hello world
 *
 * Return: Always 0.
 */
int main ()
{
	char hello[] = 'h', 'e', 'l', 'l', 'w', 'o', 'r', 'l', 'd';

	for(i = 0; i < hello[i]; i++)
	{
		_putchar(hello[i]);
	}
	return (0);
}
